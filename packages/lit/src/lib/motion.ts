import { animate, animateStyle, stagger, timeline } from "motion";

/**
 * Motion class to provide motion utilities.
 */
export class Motion {
  animate = animate;
  animateStyle = animateStyle;
  stagger = stagger;
  timeline = timeline;
}
